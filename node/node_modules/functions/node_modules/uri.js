module.exports = function uri(args){
	let auth = args.auth ? `${args.username}:${args.password}@` : ''
	ret = `mongodb+srv://${auth}${args.ip}/${args.dbName}?retryWrites=true&w=majority`
	return ret
}